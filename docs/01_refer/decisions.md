# Velo — Decisions Log

> Flat replacement for ADR hierarchy. One row per meaningful decision.
> Consumed by: anyone planning work that might conflict with a past decision.
> Updated: 2026-04-28 (S1-Clean-Sync).

| # | Date | Decision | Why | Where it lives | Status |
|---|---|---|---|---|---|
| 001 | 2026-04-23 | Adopt SPEC v3.2-velo reduced profile | Full SPEC v3.2 is over-scaled for single-dev frontend project | `ARCHITECTURE.md` § Framework Profile | ACTIVE |
| 002 | 2026-04-23 | Use Claude Design for every screen in Sprint 1 pilot | Learn the tool fully before committing to a rollout strategy | `03_Phase-Builder.md` § Design-Gen, Sprint 1 plan | ACTIVE |
| 003 | 2026-04-23 | No separate `api-contract.md` | `frontend/src/api/types.ts` is the SSOT for frontend | `ARCHITECTURE.md` § Out of Scope | ACTIVE |
| 004 | 2026-04-23 | Drop protocols 06/07/Spec-Install from active set | Framework maintenance not applicable; first-install is this plan | `02_spec/` (active) + `05_legacy/_original_v3.2.0/` | ACTIVE |
| 005 | 2026-04-23 | Single ARCHITECTURE.md instead of separate VISION / ROADMAP | Project is small; splits add overhead without value | `ARCHITECTURE.md` | ACTIVE |
| 006 | 2026-04-24 | Bundle (2026-04-23) = SSOT of design system | Bundle is newer than Design_prototype (2026-03-11); bundle README declares pixel-perfect recreation approach | `ARCHITECTURE.md` §Tools + §Out of Scope; `docs/04_assets/velo-design-system-2026-04-23/` | ACTIVE |
| 007 | 2026-04-24 | Flat aesthetic; no backdrop-filter / no glassmorphism | Bundle README explicitly declares "No backdrop-filter: blur"; performance on Telegram WebView | `ARCHITECTURE.md` §Brand-lock; variables.css cleanup in C04 | ACTIVE |
| 008 | 2026-04-24 | Dark mode in scope S1 — tokens only; UI toggle infrastructure deferred to C19 (S2) | Bundle provides full dark-mode tokens via `[data-theme="dark"]`; stores/ui.ts needs theme field + localStorage + prefers-color-scheme listener | `ARCHITECTURE.md` §Coding Standards Theme support; C02 (tokens), C19 (UI) | ACTIVE |
| 009 | 2026-04-24 | Token rename to bundle namespace; DESIGN_MIGRATION.md v4 SUPERSEDED | Bundle introduces new token names (`--text-primary`, `--surface-default`, etc.). Maintaining velo-prefixed names requires ongoing translation layer; trade-off favors bundle compliance | `ARCHITECTURE.md` §Coding Standards; `docs/05_legacy/_archive/DESIGN_MIGRATION_v4_2026-04-12.md`; C03 grep/replace 577 usages | ACTIVE |
| 010 | 2026-04-24 | Admin views (7) + MH-08 + MH-11 + MH-12 → BACKLOG for S4+ | 3-sprint capacity fits user-core + master-core + MH-17 only; admin has no MH card | `BACKLOG.md` cards 4–7; `S3-SPRINT.md` Out of Scope | ACTIVE |
| 011 | 2026-04-24 | 3 sprints planned in one Sprint-Builder session (S1 + S2 + S3) | Single-dev continuity; scout-driven planning benefits all sprints together; risk of carry-forward handled via C14 retrospective | `S2-SPRINT.md` Status header; `S3-SPRINT.md` Status header; Sprint-Builder v3.2-velo deviation | ACTIVE |
| 012 | 2026-04-24 | Bundle AuthScreen NOT copied 1:1; Velo remains Telegram-first | Bundle AuthScreen shows Google/Apple OAuth which Velo does not use (TMA-only auth via initData). Bundle AuthScreen retained as landing/desktop reference | C11 WelcomeView design-gen; C26 TMA Auth polish; `frontend/src/platform/telegram.ts` | ACTIVE |
| 013 | 2026-04-24 | VELO = Telegram Mini App with PWA fallback for non-Telegram browsers | Confirmed via `frontend/src/platform/` (telegram.ts + standalone.ts + App.vue routing); vite-plugin-pwa in deps; no `@telegram-apps/sdk` npm pkg (uses global `window.Telegram.WebApp`) | `ARCHITECTURE.md` §Project Overview; `frontend/src/platform/*` | ACTIVE |
| 014 | 2026-04-24 | Phases numbered globally across project (P01–P12 in S1–S3 plan) | Matches cycle numbering rule; avoids phase-number collision across sprints | `01_Declaration.md` §Cycle Numbering Rule (applies to phases by analogy); S1/S2/S3 SPRINT files | ACTIVE |
| 015 | 2026-04-25 | Phase 01 replanning during execution | Scout consistently revealed scope larger than sprint plan estimate (velo count 633 vs ~577; glass scope ~263 edits vs "6 tokens"); structural adjustments preserved sprint goal while respecting reality. Removed original C06 (pre-empted by Sprint-Builder commit `9cf88fa`); reclassified C04 MEDIUM→HIGH; inserted Pre-C02/Pre-C03 mini-scouts; split C03 into Assess+Apply per Rule 16 | Phase 01 execution record; future Sprint-Builder reference for scout-driven replanning | ACTIVE |
| 016 | 2026-04-25 | Nine conflict tokens adopt bundle values (C02) | Bundle is SSOT per #006. Mini-scout confirmed value-safety: all 29 `--radius-full` usage sites below 64px (200px and 9999px render identically); bundle shadow values bring real elevation matching bundle "light-mode hierarchy via shadow" intent | `frontend/src/styles/variables.css` C02; per-site migration (62 `--radius-md` cards → `--radius-lg`; 3 `--radius-sm` warnings → `--radius-md`) at C03 | ACTIVE |
| 017 | 2026-04-25 | Decision #007 clarification: shadows permitted | #007 read narrowly: forbidden = `backdrop-filter`, `-webkit-backdrop-filter`, glassmorphism, semi-transparent blur-layered surfaces; permitted = `box-shadow` with bundle `--shadow-*` tokens. Bundle README explicitly uses shadows as light-mode hierarchy carrier; #006 SSOT | `decisions.md` (clarifies #007); applied in C02 + C04 | ACTIVE |
| 018 | 2026-04-25 | npm lockfile implicit scope | Cycles with `npm run typecheck/lint/test/build` in Acceptance Criteria implicitly include `frontend/package-lock.json` in Scope. npm install may normalize lockfile (peer markers, metadata) without version changes; such diffs commit as part of cycle's phase. C01 exhibited this as systemic, not per-cycle | `ARCHITECTURE.md` §Coding Standards "Build & dependency tooling"; applied retroactively from C01 | ACTIVE |
| 019 | 2026-04-25 | CSS loading: main.ts imports, not @import | Token + global CSS files imported via JavaScript module graph in `frontend/src/main.ts` lines 16-17, NOT via CSS `@import`. Standard Vite/Vue convention for bundler optimization. C02 Pre-Exec literal-STOP misfired on incorrect `@import` assumption | `ARCHITECTURE.md` §Coding Standards "CSS architecture"; protocol prompts verify token availability via module import order | ACTIVE |
| 020 | 2026-04-25 | Admin-deferred marker convention | Tokens used exclusively by admin views (deferred per #010) remain in variables.css Legacy section with explicit marker comment block listing using files. Renaming admin-only tokens in C03 would create 8 project-extension tokens with zero active consumers until S4+. Marker signals intent + blocks re-BACKLOG | `frontend/src/styles/variables.css` Legacy section (8 tokens at lines 234-241); S4+ admin-reactivation cycle removes them with associated admin-view usages | ACTIVE |
| 021 | 2026-04-25 | Project-extension token pattern | When bundle has no equivalent for a velo token still needed (single-use semi-transparent values, layout frames, admin-deferred feedback variants), create project-extension token in variables.css Bundle section using bundle naming with project-specific identifier. FP-01 compliant (no inline hex/rgba), preserves semantic clarity | `frontend/src/styles/variables.css` lines 47-57 (`--nav-inactive-bg` + 5 alpha-surface variants); pattern template for future project-specific extensions | ACTIVE |
| 022 | 2026-04-26 | API contract patch C06 reduced scope after Pre-Exec discovery | Backend partner's contract review (`Zodd_review.md` @ `105fddd`) identified 4 frontend findings vs commit `364893d`. C06 Pre-Exec discovered: types are auto-generated (`generated.ts` from `backend/scripts/generate_ts_types.py`); B.1 already resolved by partner regen at `81304a6` (generated.ts `UserResponse:684-697` matches audit AFTER shape; 0 stale-usage); A.2 blocked — backend shipped CR-01 (`masters/schemas.py:117-141` Pydantic + `masters/router.py:81-82` population) but regen ran against stale openapi.json (no fields in `MasterProfileResponse`). C06 scope reduced 4→2 fixes (B.3 P1 + B.11 hygiene); B.1 documented as already-resolved; A.2 + 38 other typecheck errors deferred to C06b post-regen consumer migration. Functional drift: zero (constants 50/2 mirror backend settings 5000/200) | `UserDashboardView.vue` + `client.ts` modified in C06; 11 files in C06b; BACKLOG entries created for A.2 follow-up + Zodd CRITICAL #1 timezone + audit-snapshot fingerprint convention | ACTIVE |
| 023 | 2026-04-26 | types.ts SSOT reinterpretation post-regen pipeline; Option 3 broaden as standard | Partner regen pipeline at `81304a6` introduced `frontend/src/api/generated.ts` (auto-gen from backend OpenAPI via `backend/scripts/generate_ts_types.py`, header "DO NOT EDIT MANUALLY"). `types.ts` is now re-export hub for backend-derived types + local declarations for frontend-only union types (PracticeType, PracticeStatus, BookingStatus, WithdrawalStatus, etc.). For post-regen consumer migration where backend widens previously-narrow union types: prefer Option 3 (broaden `Record<NarrowType, X>` → `Record<string, X>` and function params from union → `string`) over Option 1 (cast-at-boundary `as Type`) or Option 2 (centralized helpers). Aligns with existing `MOOD_EMOJI`/`RATING_EMOJI`/`METHOD_LABEL` convention; no signature lies; runtime safety preserved via existing `?? fallback` patterns. Reinterprets #003 — `types.ts` retains SSOT for frontend-only types; `generated.ts` is de-facto SSOT for backend-derived shapes | `ARCHITECTURE.md` §Components updated; C06b applied across `displayHelpers.ts`, `PracticeListItem.vue`, `BookingCard.vue`, `AnalyticsView.vue`, `EditPracticeView.vue`, `MasterDashboardView.vue`, `MasterFinanceView.vue`, `api/masters.ts`, `MasterProfileView.vue` | ACTIVE |
| 024 | 2026-04-26 | Icons strategy: Vue-SVG baseline + bundle PNG decorative supplement (D3 ratified) | Vue-SVG обеспечивает theme-awareness (S2 C19 dark mode) + scalability + no extra HTTP requests; уместно для UI primitives (nav/state/action). Bundle PNG (12 файлов) уникальны как декоративные wellness-акценты (mood-карточки, master flair, onboarding illustrations) — нет SVG-эквивалентов в Velo, замена через сложный SVG-art не оправдана сейчас. Из 2 коллизий (brain, meditation) выигрывает Vue-SVG. IconRuble flagged как кандидат на удаление (Velo на EUR — потенциальный dead import). Future SVG-migration для 10 bundle-only PNGs — backlog | `ARCHIVES/AUDIT/S1-AUDIT.md §9 Icons strategy`; `BACKLOG.md` #29 IconRuble + #30 SVG-migration; `frontend/src/components/icons/` (Velo set), `frontend/src/assets/brand-icons/` (bundle decorative) | ACTIVE |
| 025 | 2026-04-26 | C11 WelcomeView fast-track без Claude Design pipeline | WelcomeView — простой TMA-only splash (мандала + VELΘ wordmark + tagline + 1 CTA "Открыть в Telegram"). Claude Design pipeline не даёт уникальной ценности для статичного landing с одной кнопкой; bundle WelcomeScreen.jsx + StandaloneStubView.vue + decisions #012/#013 покрывают визуальный и функциональный constraint. Decision #002 ("every screen via Claude Design in S1 pilot") — explicit deviation для C11 принято на S1 P03 OPEN §3 design-gen precondition gate (handoff отсутствовал, Welcome простой по природе, pipeline учится на C10 Dashboard merge — более показательный и сложный кейс). Будущие greenfield экраны S2/S3 проходят Claude Design по умолчанию | `decisions.md` #002 (deviation explicit для одного экрана); `frontend/src/views/auth/WelcomeView.vue` (123 LOC artefact); S1 P03 C11 cycle result inline в `S1-SPRINT.md` | ACTIVE |
| 026 | 2026-04-26 | ProbeKit lite + test-suite hardened to Velo paths; brand_ref repointed to bundle SSOT; no BOGame version pulls warranted | First lite-profile run on Velo. Scout (S1-Sprint-Closer Step 1+) revealed 9 CBS-HOME hardcoded path fields across 4 skills (type-audit / a11y-audit / responsive-audit / design-audit), 10 cosmetic CBS prose references, and a dangling `brand_ref` pointer (`.claude/skills/livemockup-studio/reference/brand-cbs.md` does not exist anywhere on disk). BOGame skill registry was checked as a possible upstream — every lite + optional skill is byte-identical with Velo's at SKILL.md level (test-suite is Velo NEWER 4.0.0 vs BOGame 3.4.0); no version pulls beneficial. Patches: 5 SKILL.md files (4 lite + test-suite report_dir typo). brand_ref repointed to `docs/04_assets/velo-design-system-2026-04-23/project/README.md` per #006 (bundle SSOT). probekit-i18n-audit deliberately NOT patched — i18n infrastructure does not exist in Velo; harden when i18n lands (BACKLOG entry added). | `.claude/skills/probekit-{type,a11y,responsive,design}-audit/SKILL.md` + `.claude/skills/probekit-test-suite/SKILL.md`; `BACKLOG.md` i18n-audit harden entry | ACTIVE |

| 027 | 2026-04-30 | Self-deploy capability via `velo update` on staging | Backend partner provided server access (37.1.204.171) + `velo update` command that pulls latest from `new_desing` branch. Eliminates BACKLOG #39 as blocker | `ENVIRONMENT.md`; `SERVER-ACCESS.md` (gitignored, populated 2026-04-30) | ACTIVE |
| 028 | 2026-04-30 | Hybrid auth model (γ): TMA primary + email/OAuth for PWA standalone | New design batch shows Login/Register/OAuth screens which contradict pure TMA-only #012/#013. Resolution: TMA users skip Welcome → use initData; PWA users see Welcome → email/OAuth UI (mock until backend lands). Partial supersede of #012 (auth screens now exist for PWA context only) | `ARCHITECTURE.md` §Project Overview; `frontend/src/views/auth/`; `BACKEND-COORDINATION.md § A.1, A.2`; `DESIGN-DECISIONS-LOG.md § A.1` | ACTIVE |
| 029 | 2026-04-30 | New design batch (2026-04-30) supersedes bundle for visual layer; bundle remains SSOT for tokens (~80% retained) | Designer delivered 55-mockup batch superseding bundle screen designs. Bundle tokens (colors, typography, surfaces) ~80% reused; component patterns refreshed (chat-style composer, ornate timeline spine, mood iconography, etc.). Bundle reference path remains for token lookups | `ARCHITECTURE.md` §Components; `DESIGN-DECISIONS-LOG.md § A` (catalog of accepted decisions); image inventory `velo_screens_inventory.md` (offline analysis artifact) | ACTIVE |
| 030 | 2026-04-30 | User role split S2+S3; Master role start in S4 contingent on designer batch | New design batch surfaces ~34 user-facing unique views. S1 sprint-closer signal: quality > density. Splitting user role into S2 (foundation + booking) + S3 (content + diary + messages) keeps cycles ~20 each. Master role waits for separate design batch from designer | `S2-SPRINT.md` § Goal + Out of Scope; `S3-SPRINT.md` § Goal + Out of Scope; `DESIGN-DECISIONS-LOG.md § A.20` | ACTIVE |
| 031 | 2026-04-30 | Regen pipeline trigger model: manual on partner signal | Backend partner signals when openapi changes (Telegram/Slack); frontend triggers `python backend/scripts/generate_ts_types.py` in next active cycle; consumer migrations follow same cycle. Pre-commit auto-regen deferred to future when partner velocity warrants | `BACKEND-COORDINATION.md § D` | ACTIVE |
| 032 | 2026-04-30 | Diary layout toggle = single view + state (Variant A) — not separate routes | Diary timeline ↔ list visualizations are layout state of one view, persisted in localStorage. Velo has no shareable diary URLs; deep-linking unnecessary. Reduces cycle count + avoids logic duplication | `frontend/src/views/user/DiaryView.vue` (S3 C36); `DESIGN-DECISIONS-LOG.md § A.7` | ACTIVE |
| 033 | 2026-04-30 | Diary entry type via frontend filter chip + backend extension queued | Дневник vs Сонник = entry type, not separate views. Backend `DiaryEntry.type` enum required (journal/dream/insight; default journal); frontend works on placeholder type until backend lands | `frontend/src/views/user/EntriesView.vue` (S3 C39 + C41); `BACKEND-COORDINATION.md § B.1`; `DESIGN-DECISIONS-LOG.md § A.8` | ACTIVE |
| 034 | 2026-04-30 | Onboarding completion persistence — localStorage v1 | localStorage flag `velo:onboarding_completed`; per-device persistence; lost on PWA reinstall (rare). Set after finalization in timezone-capture view (end of carousel + timezone flow). Server-side `User.onboarding_completed` deferred to future if cross-device parity needed | `frontend/src/views/auth/OnboardingCarouselView.vue` (S2 C20) + `frontend/src/views/auth/OnboardingTimezoneView.vue` (S2 C21, where flag is set); `BACKEND-COORDINATION.md § B.2`; `DESIGN-DECISIONS-LOG.md § A.2` | ACTIVE |
| 035 | 2026-04-30 | Mid-practice "Check-in" = upsert (re-open pre-practice form), no new endpoint | PracticeLive (14) "Check-in" button reopens form 12; existing `POST /practices/{id}/checkin` is upsert; mood overwrite, not new event. Avoids backend extension for ambiguous UX | `frontend/src/views/user/PracticeLiveView.vue` (S2 C31); `BACKEND-COORDINATION.md § C.4`; `DESIGN-DECISIONS-LOG.md § A.10` | ACTIVE |
| 036 | 2026-04-30 | WelcomeView splits into TMA-splash branch + PWA-standalone branch | TMA users: detected via platform/index.ts → skip Welcome → loading + Dashboard. PWA users: see Welcome (01) with Войти / Создать аккаунт CTAs leading to Login/Register/OAuth | `frontend/src/views/auth/WelcomeView.vue` (S2 C16; refactored from S1 C11); `frontend/src/platform/index.ts`; `DESIGN-DECISIONS-LOG.md § A.1` | ACTIVE |
| 037 | 2026-04-30 | Practice Live "Войти" = external Zoom link (window.open) | No embedded Zoom SDK; tap "Войти" opens Zoom client externally via `practice.zoom_link`. Simplest UX; no integration complexity | `frontend/src/views/user/PracticeLiveView.vue` (S2 C31); `DESIGN-DECISIONS-LOG.md § A.10` | ACTIVE |
| 038 | 2026-04-30 | Avatar read-only — Telegram-managed | Backend openapi excludes avatar from UserUpdate ("managed by Telegram"). Frontend EditProfileView shows avatar but tap → toast "Telegram-managed" | `frontend/src/views/user/EditProfileView.vue` (S2 C34); `DESIGN-DECISIONS-LOG.md § A.11` | ACTIVE |
| 039 | 2026-04-30 | Topup flow remains S1-as-is in S2 (designer didn't include in batch) | Existing TopupView / TopupSuccessView / TopupCancelView from S1 unchanged in S2. Designer didn't ship topup screens in 2026-04-30 batch. Future redesign queued in DESIGN-DECISIONS-LOG § B.14 | Existing files in `frontend/src/views/user/`; `DESIGN-DECISIONS-LOG.md § A.19, § B.14` | ACTIVE |
| 040 | 2026-04-30 | Booking endpoint defaults to /purchase for all practices (paid + free) | Unification rationale: backend speccs handle paid_cents=0 in purchase; one endpoint for both flows simplifies frontend. Pending partner confirmation; if partner says "free → /bookings" → revisit | `frontend/src/api/bookings.ts`; `BACKEND-COORDINATION.md § C.1` (P0) | ACTIVE pending partner confirm |
| 041 | 2026-04-30 | Coordination doc format established: BACKEND-COORDINATION.md (partner) + DESIGN-DECISIONS-LOG.md (designer/PM/sponsor) | Two artifacts persisting cross-team coordination: missing endpoints/schema extensions/clarifications for backend; accepted decisions/open questions/awaited batches for design. Both live updates in `docs/03_sprint/S2-bundle-port/`; survive across S2 and S3 | `docs/03_sprint/S2-bundle-port/BACKEND-COORDINATION.md` + `DESIGN-DECISIONS-LOG.md` | ACTIVE |
| 042 | 2026-04-30 | Sprint scope discipline: quality > density — S2/S3 split user role across two sprints | S1 retro signal: density caused quality issues (audit found 1 CRITICAL + 3 HIGH). Re-plan honors this by per-cycle staging push + per-screen testing rhythm; user role spans S2 (foundation) + S3 (content) instead of compressing into one sprint | `S2-SPRINT.md` § Goal; `S3-SPRINT.md` § Goal | ACTIVE |

| 043 | 2026-04-30 | Human-only-relay execution model — all commands through Claude Code | Human's permanent preference made explicit. Human never executes terminal/git/SSH/file commands directly. Workflow: Claude Chat writes execute prompt → Human pastes to Claude Code → Claude Code executes + reports → Human pastes report back to Claude Chat. Claude Code is execution advisor with direct material access; STOPs on ambiguity for clarification through Human relay. Decisions remain Human + Claude Chat in chat. Implication: prompts that require interactive auth (password input) need non-interactive alternative (SSH key, env var, etc.) or partner coordination — never Human terminal action | `docs/02_spec/01_Declaration.md` Rule 26 + Rule 27; affects all future execute prompt design | ACTIVE |

| 044 | 2026-04-30 | paramiko как primary SSH primitive для Claude Code (ssh.exe sandboxed на Windows) | Claude Code sandbox блокирует прямой вызов `ssh.exe` (OpenSSH Windows client) — наблюдалось как silent permission denied на connect, до auth слоя. plink (PuTTY) работает но требует .ppk format key + puttygen для конверсии нативно зависает в MSYS Git Bash из-за interactive tty requirement. paramiko (pure-Python SSH library, `pip install paramiko`) обходит обе проблемы — sandbox-friendly + читает OpenSSH-format keys нативно. Future server operations использовать paramiko как primary, plink+pwfile как last-resort password fallback | `docs/01_refer/ENVIRONMENT.md` § Test Infrastructure → Staging server (snippet template) | ACTIVE |
| 045 | 2026-04-30 | SSH key auth как стандарт для staging access; password retired до emergency-fallback role | Установлен ed25519 keypair в `~/.ssh/velo_staging_ed25519` (no passphrase per Rule 26 — passphrase = re-introduction of interactive prompt). Public key добавлен в server `/root/.ssh/authorized_keys` через one-time plink+password операцию. SSH config alias `velo-staging` в `~/.ssh/config`. Password в SERVER-ACCESS.md помечен как deprecated (emergency-only). Future deploys полностью hands-off через paramiko + key | `~/.ssh/velo_staging_ed25519` (private, outside repo); `docs/01_refer/SERVER-ACCESS.md` (gitignored, deprecation note) | ACTIVE |
| 046 | 2026-04-30 | Self-host backend dev stack for openapi regen when partner stalls (fallback to #031) | BACKEND-COORDINATION § E claimed three S1 fixes resolved at `81304a6`, but S2 P05 pre-flight scout found the corresponding fields still absent in `frontend/src/api/generated.ts` — partner had patched Pydantic schemas but not re-run regen against fresh openapi (decision #022 noted this risk). Frontend may now self-host `docker compose up -d postgres redis app` (no `frontend` container, no migrations) to dump `/openapi.json` and run `backend/scripts/generate_ts_types.py`. FastAPI generates `/openapi.json` from Pydantic introspection at app import time, independent of DB state — `/ready` may stay 503, that's expected. Recipe: `docker compose up -d postgres redis app && sleep 15 && curl -sS --fail http://127.0.0.1:8000/openapi.json > /tmp/velo-openapi.json && python backend/scripts/generate_ts_types.py /tmp/velo-openapi.json frontend/src/api/generated.ts && docker compose down && rm /tmp/velo-openapi.json`. Does NOT extend to editing backend Python; missing Pydantic fields still escalate to partner per § D. Reinterprets #031 — that decision describes the preferred path (partner-signal-driven regen); #046 is the fallback when § E claims drift from `generated.ts` reality | `decisions.md` (this row); first applied S2 P05 C15 (self-host regen + 3 corrected § E entries + 3 newly-surfaced partner fixes E.1-E.3) | ACTIVE |
| 047 | 2026-04-30 | Path Y — logic-first build mass; visual polish deferred to S5+ cluster | S2-P06 cycles confirmed Code direct-port from skin PNG produces working SFC + correct logic faster than Claude Design pipeline gen→handoff→wire-up. Trade-off: visual fidelity MEDIUM (acceptable, not pixel-perfect). Polish cluster scheduled S5+ as dedicated sprint work. Inherits #042 quality-vs-density principle but inverts at execution stage — quality-of-logic preserved, quality-of-visuals deferred. First applied: S2-P06-C16 (2026-04-30); confirmed across 6 cycles (C16-C21); inherited by speedrun MEGA-1/MEGA-2 across C22-C52 | Phase 06 cycle commits cc4e2fd / b060ba3 / de496f6; speedrun commits 6c5fd1f (MEGA-1) / af39b41 (MEGA-2); `ARCHITECTURE.md` §Phase 06-13 additions; this row | ACTIVE |
| 048 | 2026-04-30 | No-emoji rule + icon-component discipline | Bare emoji characters in UI are inconsistent with Velo brand voice (Marmelad serif + cool blue palette + flat surfaces). Replaced with inline SVG icons via `frontend/src/components/icons/Icon*.vue` baseline (currentColor + viewBox 24×24, `:size` prop default 24). First applied speedrun MEGA-1 (~74 in-scope hits cleaned across user views) + MEGA-2 (BACKLOG #98 carry-over resolved — DiaryList + BookingCard + PracticeCard + FormShell + CancelBookingPopup + DiaryCheckin/Feedback/EntryDetail + DiaryEntryForm + MyBookingsView + TopupCancel/SuccessView + adminHelpers). Master/admin views still hold legacy emoji glyphs (S4/S5+ cleanup scope). | `frontend/src/utils/displayHelpers.ts` PRACTICE_TYPE_ICON map (refactored from PRACTICE_TYPE_EMOJI; deprecated emoji exports kept as compat shims for unconverted callers); `frontend/src/components/icons/` (38 components post-MEGA-2: 13 baseline + 11 MEGA-1 + 14 MEGA-2 — Mic/Check/BookDream/BookFeather/Link/ChevronDown/Question/Bell/Globe/Shield/Share/Logout/Search/Trash); cleanup pattern documented for future cycles | ACTIVE |
| 049 | 2026-04-30 | Speedrun mode — explicit inversion of #042 (quality > density) for sponsor-demo target | 33 cycles (C22-C54) executed across 2 mega-execute prompts + 2 commits + 1 closure commit. Per-cycle Claude Design pipeline + per-cycle visual verify rhythm collapsed into 2 aggregate visual verify gates (post-MEGA-1, post-MEGA-2). Audit ceremony (Sprint-Closer Step 1+ ProbeKit lite) deferred to dedicated post-demo cycle (BACKLOG #100). Quality backfill scheduled S5+ polish cluster. Trade-off explicit: shipping flow + functionality for demo, audit + retro rigor + pixel polish backfilled prior to production promotion. | `S2-SNAPSHOT.md` + `S3-SNAPSHOT.md` document throughput metrics; `BACKLOG.md` #100 reactivation task; closure commit message `sprint: S2 + S3 — CLOSED — speedrun (sponsor-demo target)` | ACTIVE |

Status values: ACTIVE, SUPERSEDED (by #NNN), DEPRECATED.
