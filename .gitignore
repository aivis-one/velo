.DS_Store
.idea/
desc/

# Secrets & Environment — safety net for root-level .env files.
# Subdirectories (backend/, frontend/) have their own .gitignore
# that also excludes .env, but this catches misplaced files.
.env
.env.*
/TMP/

# Design-system reference screenshots — kept locally for design verification,
# not committed (heavy PNGs, 80+ MB; regenerated from Figma when needed).
docs/02_design-system/assets/screenshots/
